variables:
    GIT_SUBMODULE_STRATEGY: normal
    TAG_IMAGE: 0

stages:
  - build
  - test

.launch_rule:
    interruptible: true
    only:
        - merge_request
        - main
        - /^rel\/.*$/

include:
    - ci/build.yml
    - ci/test.yml
